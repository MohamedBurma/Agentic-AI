# COSC726 Lab 7 Ledger

سيتم توليد جدول القياس الفعلي بعد تشغيل:

```bash
python crew.py
```

يتضمن الجدول لكل brief: عدد الـ tokens، الزمن، coverage، grounding، ونسبة استهلاك الـ crew مقارنة بالـ baseline.

## النتائج

سيستبدل البرنامج هذا الملف تلقائيًا بالنتائج الفعلية عند نجاح الاتصال بـ Ollama وتشغيل النموذج `qwen2.5:7b`.
